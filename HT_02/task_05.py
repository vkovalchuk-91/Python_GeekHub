# Write a script which accepts decimal number from user and converts it to hexadecimal.

inputted_number = int(input("Input decimal number: "))
print(f"Hexadecimal value of inputted number: {hex(inputted_number)}")
